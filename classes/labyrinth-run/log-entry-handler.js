const logEntryHandler = (currentState, nextState, logEntry) => {
  /*
  if (currentState === 'ready') {
    // if log entry is an izaro line
    // start timing
  }
  switch (nextState) {
    case 'ready':
      // do a thing
      break;
    case 'izaro':
      // do a thing
      break;
    case '
  */
};
